================================================================================
                    CACIQUE ANALYTICS - COMANDOS RÁPIDOS
                         Referencia de operación
================================================================================

═══════════════════════════════════════════════════════════════════════════════
1. POSTGRESQL - CONTROL DEL SERVICIO (Windows)
═══════════════════════════════════════════════════════════════════════════════

OPCIÓN A: VS Code Tasks (recomendado - Ctrl+Shift+P)
────────────────────────────────────────────────────
Tasks: Run Task → PostgreSQL: Start
Tasks: Run Task → PostgreSQL: Stop
Tasks: Run Task → PostgreSQL: Status
Tasks: Run Task → PostgreSQL: Startup Manual


OPCIÓN B: PowerShell directo (abrir como administrador para start/stop)
──────────────────────────────────────────────────────────────────────

Iniciar servicio:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/postgres-start.ps1

Detener servicio:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/postgres-stop.ps1

Ver estado:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/postgres-status.ps1

Configurar startup en Manual:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/postgres-service.ps1 -Action manual


═══════════════════════════════════════════════════════════════════════════════
2. POSTGRESQL - CONEXIÓN Y OPERACIONES EN PSQL
═══════════════════════════════════════════════════════════════════════════════

Verificar versión instalada:
psql --version

Conectar como superusuario postgres (pedirá password):
psql -U postgres -h localhost

Conectar como usuario de aplicación (después de crearlo):
psql -U cacique_app -h localhost -d cacique_analytics


NOTA: Si psql no se reconoce, agrega al PATH:
$env:PATH += ";C:\Program Files\PostgreSQL\18\bin"

Para hacerlo permanente (PowerShell como admin):
[Environment]::SetEnvironmentVariable("PATH", $env:PATH + ";C:\Program Files\PostgreSQL\18\bin", "User")
(Luego cierra y reabre PowerShell)


═══════════════════════════════════════════════════════════════════════════════
3. POSTGRESQL - CREAR BASE DE DATOS Y USUARIO DE APLICACIÓN
═══════════════════════════════════════════════════════════════════════════════

(Ejecutar dentro de psql, conectado como postgres)

Crear base de datos:
CREATE DATABASE cacique_analytics;

Conectar a la base de datos:
\c cacique_analytics

Crear usuario de aplicación (reemplazar con contraseña real):
CREATE USER cacique_app WITH PASSWORD 'tu_password_robusta_aqui';

Otorgar permisos de conexión:
GRANT CONNECT ON DATABASE cacique_analytics TO cacique_app;

Otorgar uso del schema public:
GRANT USAGE ON SCHEMA public TO cacique_app;

Otorgar privilegios por defecto para tablas futuras:
ALTER DEFAULT PRIVILEGES IN SCHEMA public
    GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO cacique_app;

Salir de psql:
\q


═══════════════════════════════════════════════════════════════════════════════
4. COPILOT MODEL ADVISOR - SELECTOR DE MODELOS
═══════════════════════════════════════════════════════════════════════════════

OPCIÓN A: VS Code Tasks (recomendado - Ctrl+Shift+P)
────────────────────────────────────────────────────
Tasks: Run Task → Copilot: Model Advisor (quick_question)
Tasks: Run Task → Copilot: Model Advisor (docs)
Tasks: Run Task → Copilot: Model Advisor (refactor_simple)
Tasks: Run Task → Copilot: Model Advisor (architecture)
Tasks: Run Task → Copilot: Model Advisor (etl_critical)
Tasks: Run Task → Copilot: Model Advisor (debug_complex)
Tasks: Run Task → Copilot: Model Advisor (code_review)


OPCIÓN B: PowerShell directo
─────────────────────────────
Preguntas rápidas (ahorra tokens):
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/copilot-model-advisor.ps1 -TaskType quick_question

Documentación:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/copilot-model-advisor.ps1 -TaskType docs

Refactor simple:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/copilot-model-advisor.ps1 -TaskType refactor_simple

Arquitectura (critico):
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/copilot-model-advisor.ps1 -TaskType architecture

ETL critico:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/copilot-model-advisor.ps1 -TaskType etl_critical

Debug complejo:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/copilot-model-advisor.ps1 -TaskType debug_complex

Code review:
powershell -NoProfile -ExecutionPolicy RemoteSigned -File scripts/copilot-model-advisor.ps1 -TaskType code_review


═══════════════════════════════════════════════════════════════════════════════
5. TABLA DE ENRUTAMIENTO DE MODELOS (reference)
═══════════════════════════════════════════════════════════════════════════════

TaskType             Modelo Recomendado              Cuando Usar
────────────────────────────────────────────────────────────────────────────
quick_question       Claude Haiku 4.5 (preferido)    Preguntas conceptuales
docs                 Claude Haiku 4.5 o Sonnet 4    Documentación/redacción
refactor_simple      Claude Sonnet 4.5 o GPT-5      Cambios mecánicos
architecture         GPT-5.3-Codex                  Diseño de BD, pipelines
etl_critical         GPT-5.3-Codex                  Idempotencia, ETL
debug_complex        GPT-5.3-Codex o Sonnet 4.6     Fallos complejos
code_review          GPT-5.3-Codex o Sonnet 4.6     Revisión antes de commit


═══════════════════════════════════════════════════════════════════════════════
6. COMANDOS GIT - COMMITS Y CONTROL
═══════════════════════════════════════════════════════════════════════════════

Ver estado del repositorio:
git status

Ver cambios sin stagear:
git diff

Ver cambios en un archivo:
git diff README.md

Stagear cambios específicos:
git add archivo.py
git add .github/
git add *.txt

Stagear todo:
git add -A

Ver commits recientes:
git log --oneline -10

Deshacer cambios en un archivo (sin stagear):
git restore archivo.py

Deshacer cambios en un archivo (ya stageado):
git restore --staged archivo.py

Crear un commit con mensaje:
git commit -m "Fix: descripción del cambio"

Ver ramas locales:
git branch

Ver estado respecto a remote:
git status


═══════════════════════════════════════════════════════════════════════════════
7. CONVENCIONES DEL PROYECTO
═══════════════════════════════════════════════════════════════════════════════

CÓDIGO:
  - Variables y funciones en INGLÉS
  - Ejemplo: get_player_stats(), match_id, league_standings

COMENTARIOS Y DOCUMENTACIÓN:
  - En ESPAÑOL (para legibilidad local)
  - Ejemplo: # Obtiene las estadísticas del jugador desde la BD

SEGURIDAD:
  - NUNCA hardcodear contraseñas
  - Usar .env para credenciales (está en .gitignore)
  - No usar superusuario postgres en pipelines
  - Usar usuario cacique_app con privilegios mínimos

ARCHIVOS:
  - .env → credenciales (NO a git)
  - .env.example → template de variables (SÍ a git)
  - credenciales_temp.txt → nota personal (NO a git)


═══════════════════════════════════════════════════════════════════════════════
8. ESTADO ACTUAL DEL PROYECTO (actualizado 2026-03-15)
═══════════════════════════════════════════════════════════════════════════════

✓ PostgreSQL 18.3 instalado y operativo
✓ Scripts de control (start/stop/status)
✓ VS Code tasks configuradas
✓ Documentación de PostgreSQL completa
✓ Model advisor para selector de modelos
✓ Políticas de Copilot y códico en .github/

⏳ PRÓXIMOS PASOS:
   1. Configurar startup en Manual (sección 4 postgresql_windows_guide.md)
   2. Crear base de datos y usuario de aplicación (sección 7)
   3. Crear .env.example y .env (sección 8)
   4. Validar scripts end-to-end
   5. Definir arquitectura de tablas antes de DDL


═══════════════════════════════════════════════════════════════════════════════
NOTAS IMPORTANTES
═══════════════════════════════════════════════════════════════════════════════

• Siempre ejecuta "Startup Manual" UNA SOLA VEZ al iniciar el proyecto.
• Inicia PostgreSQL SOLO cuando necesites trabajar.
• Detén PostgreSQL al terminar para evitar proceso residente en background.
• Usa Model Advisor ANTES de cada tarea para elegir el modelo correcto.
• Haz commit después de cambios importantes.
• No pushees sin validar que todo funciona localmente primero.
• Los secretos van EN .env, NUNCA en código.

================================================================================
                              FIN DE REFERENCIA
================================================================================
